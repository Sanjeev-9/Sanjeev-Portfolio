// ========================================================================== // GRC Portfolio - Navigation & Interactivity Effects // ========================================================================== document.addEventListener('DOMContentLoaded', () => { const sections = document.querySelectorAll('section'); const navLinks = document.querySelectorAll('nav a'); /** * Scroll Spy: Automatically updates the navigation highlight active class * based on the visible element section currently on the viewport. */ const scrollSpy = () => { let currentSectionId = ''; sections.forEach(section => { const sectionTop = section.offsetTop; const sectionHeight = section.clientHeight; // Check if user has scrolled past the top of the section minus a baseline offset if (window.scrollY >= (sectionTop - 150)) { currentSectionId = section.getAttribute('id'); } }); // Remove active styling flag from all items, then apply to current view target navLinks.forEach(link => { link.classList.remove('active'); if (link.getAttribute('href') === `#${currentSectionId}`) { link.classList.add('active'); } }); }; // Attach tracking event listeners for continuous user viewport adjustments window.addEventListener('scroll', scrollSpy); window.addEventListener('resize', scrollSpy); });